import { ConfigI } from "../../types/base-interface-types/configType";

const DEFAULT_CONFIG: ConfigI = {
  entityName: "OmniHook",
  debug: false,
  allowEmpty: false,
  allowDefault: true,
  allowCustom: true,
};

export default DEFAULT_CONFIG;
